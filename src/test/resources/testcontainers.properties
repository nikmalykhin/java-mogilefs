# TestContainers configuration for local ARM-based development
# Allow connecting to Colima Docker socket

# Disable Ryuk cleanup container for ARM compatibility
testcontainers.ryuk.disabled=true

# Set startup timeout for slower ARM emulation
testcontainers.docker.client.strategy.timeout=180000
